Measured 2026-09-09T16:45:50.571843+00:00

Same-v41 comparison completed: cached 0.719592533 combined overall / 0.727978490 steady tok/s; gathered direct 0.608188783 / 0.647878570. Candidate ratio 0.845185, 15.48% slower across the two captured trials. Exact APK/settings/work/output/MTP and original-validator-only correction checks pass. Control trials [(0.8877664561661428, 0.0, 12067.014719), (0.6049869147002103, 0.0, 18144.001919)]. Control capture PASS; controller CLEANUP_FAILED547.130s from25sADBquery window; immediate read-only pidof rc1 empty confirms stopped. BankCLEAR insidecontroller; ownednone; originalfailureandfollowuppreserved. All13analyses +TCP/faults/operations/paddeliveryPASS. No inference gain; retaincached. Next v42 paired parking off-on at samework/recipe, defaultoff.

**We have located the major waiting paths and one concrete pad-publication stall. The precise protected-kernel cause of the remaining VM delays is still unresolved.** The goal remains 20 verified tok/s on Qwen3.8 27B Q8 through the Pixel protected VM and V100.

| Source point | Measured behavior | What is established |
|---|---|---|
| `anchor_payload.c:714,744–749` | A complete 809 MB shipment took 27.035 seconds to receive and publish; decoding waited 13.716 seconds for its next pad | Whole-file availability caused this specific stall. Smaller, authenticated 202 MB shipments removed pad waits in eight subsequent diagnostic trials |
| `shielded-wire.c:328,346` | Header/body reads dominate: total wire spans were 16.246 / 18.583 seconds per 16-token trial | The caller is waiting for exchange completion; the read location alone does not identify the kernel cause |
| `native-bridge.c`, VM send path | One complete reply waited 496.536 ms between the phone finishing its TCP read and completing the VM send | The bridge slept for 495 ms of that gap; this is beyond GPU computation |
| Protected VM reply consumption | Another exchange took 781.894 ms; the phone had finished its VM send about 5 ms after the exchange began | The guest completed the read 776.845–777.079 ms after host-side send completion. That caller had one voluntary switch, 78 microseconds CPU and zero recorded run-queue delay |

The completed TCP diagnostic produced **0.689 / 0.602 tok/s**, or **0.643 combined**, with identical expected output and MTP decisions, 2,441 offloaded nodes and 6,764 pads in each trial, and zero verification failures, local fallback, missed pads or pad wait. There is **no new throughput improvement**. Earlier two runs with smaller shipments averaged 0.866 tok/s across four trials. These instrumented runs are not production speed records.

TCP_INFO recorded **zero receive-window-limited and send-buffer-limited time** in both trial brackets. Retransmitted data was 38,780 / 15,615 bytes out of roughly 355 MB of replies per trial. These observations reject persistent window/send-buffer limitation in the measured connection; they do not prove zero link delay or quantify retransmission latency. All 7,647 snapshots and 3,823 frame pairs were retained. The new observer calls totaled 36.11 ms over the full run.

Previous source profiling measured approximately 0.380 seconds of V100 graph execution per 16-token trial. That result comes from an earlier capture and is nested within exchange waits. The latest run independently verified the worker identity, nonzero offloads and V100 memory allocation reaching 25,861 MiB. Reply-buffer reservation took only about 9 ms in its first trial.

Two previous transport changes reduced local costs but failed to improve inference: 64 KiB batching removed 84% of VM sends; 4 KiB sends removed observed host direct compaction. Both remain disabled. Eliminating unused background draft chains also reduced work without establishing a repeatable speed gain.

There is a separate capacity constraint. With the measured MTP work pattern, consumed replacement cells plus replies require at least **888 MB/s at 20 tok/s**. The existing all-group shipment format raises this conditional demand to **1,534 MB/s**. The observed 5 Gb/s USB link has a generous raw ceiling of **625 MB/s**, before overhead. The current data flow therefore also needs to change; removing the VM stalls alone cannot deliver 20 tok/s on this exact route and workload.

Validation: the TCP test and cleanup passed in 547.24 seconds. All 13 standard analyses and the TCP, operation, pad-traffic, pad-delivery and selected-exchange analyses passed. The trace retains 20,293 guest-mode samples without stacks and no other reported trace errors. Host vCPU activity does not identify which guest instructions ran.

A separate 45.29-second capability test confirmed readable guest memory usage, but denied detailed reclaim counters, pressure statistics and own kernel stacks. No permissions were changed. New commit `1cc140e6` retains page-fault counters from the profiler's existing per-thread resource snapshots, adding no per-exchange system calls. Sanitizer tests cover real page faults, invalid counters, exact replies and record overflow. The fault-correlation run completed in 523.43 seconds, including verified cleanup, at **0.830 / 0.680 tok/s** (0.747 combined). It preserved the same workload, output and MTP decisions, with no pad wait or verification failure. All 13 standard and four additional analyses passed. Its 10,230 matched fault records have zero counter errors and **zero major faults**. The latest longest waits (107 / 130 ms) have zero caller faults. The older 0.5–0.8 second extremes did not recur in this run; the new counters cannot retrospectively assign their cause. Memory pressure or faults in other guest tasks remain possible. There is no new speed record or isolated improvement. The new trace retains 18,351 guest samples without stacks and no other reported errors. All changes remain local and unpushed; production defaults are unchanged.

Fable posted substantive reviews at 16:02 and 16:03 UTC; an earlier statement that no proposal had arrived was incorrect. He identified short HTTP reads being forwarded as separate VM writes in the direct pad path. Astra implemented a default-off gathering option in local commit `75096026`; byte-count, cancellation and acknowledgment tests pass. It has no measured inference gain yet. Exact selected-stall host stacks show F2FS writes but do not establish the proposed reclaim/MMU-lock mechanism; guest stacks remain unavailable. The two agents are coordinating source review and bounded measurements through the log.

Evidence: `27b-fault-correlation-profile.json`, `27b-tcp-and-vm-stalls-profile.json`, `27b-chunk16-profile.json`, `27b-chunk16-repeat-profile.json`, `27b-head-control-profile.json`, `27b-pad-delivery-profile.md`, and `guest-resource-capability.json`.

Post-controller cleanup correction: a separate bank audit found one unconfirmed 202 MB, 16-index shipment from the stopped dealer. Under the exclusive bank lock, the production schema and current-run identity checks passed and the original child was independently verified dead. The shipment and its intent were moved to quarantine, never reused; the bank is now clear. The original 523.43-second controller receipt did not cover this later reconciliation. Subsequent runs must check the bank immediately after controller completion before reporting complete cleanup.

Direct gathering experiment: the completed 27B run produced **0.687 / 0.546 tok/s** (0.608 combined), same output/work/MTP, zero verification failures, fallback, missed pads or pad wait. Wire spans were 15.904 / 21.436 seconds. All 13 standard analyses and four additional checks passed. The original runner rejected its new configuration banner as a transfer error; the original failed receipt and exact script are preserved, and the saved capture passed the corrected full validation. Inference and phone/worker/bank cleanup took 546.68 seconds; revalidation and completed bridge-file retrieval occurred afterward. A cached control on the same v41 APK is running. This establishes no new speed improvement. Evidence: `27b-direct-fill-profile.json`.
