The app a plane serves is the component of the bundle it ADMITTED
warden-host, 2026-09-24. Run /home/steven/enclave-bench/m4b-app-085652, generated by m4/write-app-binding-evidence.sh from files in that directory.
Guest code at d094410c; hardware harness at 14eb1104 (copies with SHA256SUMS in /home/steven/enclave-bench/m4b-app-085652/harness/). One plane, plane 2.
NOT independently reviewed: the reviewer is unavailable, and nothing after 39b5a5e4 has been reviewed.

WHAT WAS OPEN. The SVSM admits the BUNDLE - sha256 of all of it is the AppID in APP_TABLE - but the plane ran
/app.wasm, a separate file build-plane-guest.sh extracted at build time. The admitted bytes and the executed bytes
were two files tied only by the build: a genuine bundle beside a different /app.wasm would be admitted, named as
the bundle's app, and serve the other one.

WHAT CHANGED. planeinit reads /app.bundle ONCE into memory and stages exactly those bytes. After the SVSM accepts
them it cuts the component from the SAME buffer (guest/appbundle.h: the contract's framing, and the manifest must
begin by naming this component's sha256), and hands it to the runtime as a SEALED memfd - write, grow, shrink and
seal all sealed - at fd 3, which the runtime opens as /proc/self/fd/3. The image carries no /app.wasm.

GOOD: THE PLANE'S OWN CUT, AND WHAT IT SERVED
  PLANE bundle=admitted
  PLANE app=cut from the ADMITTED bundle bytes: bundle_bytes=59834 manifest_bytes=231 component_bytes=59578 component_sha256=ed3dc69939f5351b2eecb05c40ea83d7bb61efcf60fa8c6a0b53ce60eba72ee6 manifest_names_it=yes sealed=write,grow,shrink,seal runtime_path=/proc/self/fd/3
  PLANE runtime_maps=ok elf_members_mapped=5/5 polls=2 outside_data=/memfd:app-component (deleted)
  served: "APP STEP2 path=/hello?from=client"
  launch digest 3418B38791325650D2BCD665915BD1ADF1C851B3A8C9235DBFADA2F19BB5D7B80526A647A92F2A4E915B65252C1357CD

verify-plane.sh check 11 compares that component_sha256 with the CONTRACT's own extractor run on the bundle the
SVSM was built to admit, and requires no /app.wasm in the runtime's maps. The only file mapping outside the runtime
set is the sealed component itself, read-only.

DECOY: A DIFFERENT COMPONENT PLANTED WHERE THE OLD PLANEINIT RAN THE APP FROM
The same image plus /app.wasm = a component compiled with label DECOY. The SVSM tables are unchanged, so the old
planeinit would have been admitted and named as the bundle's app while serving DECOY.
  PLANE stray_app_wasm=present in the image and NOT given to the runtime
  PLANE app=cut from the ADMITTED bundle bytes: bundle_bytes=59834 manifest_bytes=231 component_bytes=59578 component_sha256=ed3dc69939f5351b2eecb05c40ea83d7bb61efcf60fa8c6a0b53ce60eba72ee6 manifest_names_it=yes sealed=write,grow,shrink,seal runtime_path=/proc/self/fd/3
  PLANE runtime_maps=ok elf_members_mapped=5/5 polls=2 outside_data=/memfd:app-component (deleted)
  served: "APP STEP2 path=/hello?from=client"     (the good plane served "APP STEP2 path=/hello?from=client")

TRUNCATED: THE LAST 4 KiB OF THE BUNDLE MISSING, UNDER AN IGVM THAT COVERS THIS IMAGE AND THE GOOD RUN'S SVSM
  Hash comparison succeeded for "kernel" Hash comparison succeeded for "initrd" Hash comparison succeeded for "cmdline" 
  PLANE bundle=REFUSED: Permission denied
  PLANE refusal=ret=-22 rax_out=0x80001004
  PLANE serving=NO
  SVSM console appid lines: 0
  launch digest 7D1A8D8B81E05FE9664ECEAC2C280C9597C66BF6F07A6C8558FAC3155C235FAB702A3304DF1544435E41D1046221A095

MISSING: NO /app.bundle IN THE IMAGE
  Hash comparison succeeded for "kernel" Hash comparison succeeded for "initrd" Hash comparison succeeded for "cmdline" 
  PLANE bundle=reading the bundle failed: /app.bundle: No such file or directory
  PLANE serving=NO
  PLANE reason=the SVSM refused this plane's app bundle
  SVSM console appid lines: 0

The reason line above says "the SVSM refused" although in this case the SVSM was never asked; the bundle line says
what actually happened. The wording was corrected after this run (this commit); the scoring used the bundle line.

In both negatives nothing was admitted (no SVSM appid line at all), nothing named, no key registered, no component
handed to a runtime, and the plane powered off. Their launch digests differ from the good plane's, so a verifier
pinning the good one rejects them first; the negatives isolate admission by construction.

THE HARNESS'S OWN SCORE (run.log)
  PASS G the good plane passed every verify-plane.sh check (rc=0)
  PASS D with a decoy at /app.wasm the plane passes every check, serves EXACTLY the admitted app, and reports the decoy unused
  PASS truncated0 its SVSM is the good run's binary built for the good bundle, and its launch digest differs from the good one
  PASS truncated1 the firmware verified all three blobs, so the plane booted the image it was measured over
  PASS truncated2 nothing was admitted, named, registered or served, no component was handed to a runtime, and the plane powered off
  PASS truncated3 the SVSM REFUSED the truncated bundle with 0x80001004, the digest-mismatch code
  PASS missing0 its SVSM is the good run's binary built for the good bundle, and its launch digest differs from the good one
  PASS missing1 the firmware verified all three blobs, so the plane booted the image it was measured over
  PASS missing2 nothing was admitted, named, registered or served, no component was handed to a runtime, and the plane powered off
  PASS missing3 with no bundle the plane refused before any admission call, naming the missing file
  M4b-app-binding: all checks passed
  exit=0
  (G and D are each verify-plane.sh's eleven checks: 22 inner PASS lines, 0 FAIL)

LOCAL TESTS (no guest), test-appbundle.out, at 14eb1104
  PASS 1 the plane's SHA-256 equals sha256sum on 13 of 13 inputs, across every padding boundary and a 45 MB file
  PASS 2 the plane's cut equals the contract's extractor byte for byte, on two different bundles
  PASS 3 every malformed bundle (13 of 13 variants) is refused by the plane AND by the contract
  PASS 4 the documented divergence: an EMPTY artifact is accepted by the contract and refused by the plane
  PASS 5 the served app FOLLOWS THE BUNDLE through the sealed fd-3 path (A serves A, B serves B), with a decoy at the old /app.wasm path
  PASS 6 the runtime's component can be neither written nor truncated, even through /proc/<pid>/fd/3
  appbundle: all checks passed  (workdir /home/steven/enclave-bench/m4b-app-085652/local-test)

WHAT THIS DOES NOT ESTABLISH

  ONE app on ONE plane, one guest. Nothing here is isolation BETWEEN apps; the second-plane blocker is unchanged.

  THE CUT IS THE PLANE'S OWN WORD. planeinit performs it and prints it; the hardware attests neither. It is worth
  relying on because planeinit is inside the initrd the launch measurement covers under step 2, and it fails closed.
  The report binds the AppID (the whole bundle), not the component hash; the component hash is recomputable from
  the bundle by anyone.

  THE SEALED MEMFD IS A KERNEL PROPERTY OF THE GUEST, not a hardware one. The admitted STAGED copy is frozen by the
  SVSM; the executed copy is the sealed memfd cut from the same buffer, in guest memory the guest kernel manages.

  THE MAPS CHECK IS ONE READING AT LOAD, as in the runtime-set evidence.

  NOT INDEPENDENTLY REVIEWED.
