The verifier needs a pin and one snapshot; guestd-control/1 authenticates the control channel
warden-host, 2026-09-24. LOCAL tests only - no guest was launched for this evidence. Run /home/steven/enclave-bench/integration-tests-104622, generated by
m4/write-control-evidence.sh from files in that directory. Commit 0cef0cab798b2243c06c81bd56a780afa9eafd39.
Working tree at the run: clean
NOT independently reviewed. Nothing deployed, published, merged, released or installed; no production secret or
setting exists for this channel yet.

SUITE EXIT CODES
  contract        rc=0
  guestd          rc=0
  claim-gate      rc=0
  domain-release  rc=0
  image-repro     rc=0
  control         rc=0

1. THE VERIFIER HELPER (review points on af33b25c)
  Acceptance needs an explicit pinned release; inspection is a separate, labelled mode; the release AND the bundle
  are snapshotted once; the runtime identity is emitted from the verified snapshot; malformed arguments fail.
  PASS D1 the release is reproducible from this host's inputs: two creations, one id
  PASS D2 reconstruction from the pinned release equals the LIVE measurement of both apps of the guestd run
  PASS D3 every tampering is refused (7 of 7), the consistent rewrite only by the pinned id
  PASS D4 the vCPU count and the app each move the measurement
  PASS D5 a catalog-derived bundle is reconstructed (its own measurement), and a bare component is refused
  PASS D6 the measured front links the contract but NOT the catalog package
  PASS D7 acceptance without a pin is REFUSED; inspection is labelled on every line and has no acceptance-shaped line
  PASS D8 every mistyped or malformed pin argument fails with a usage error (6 manifest forms, 2 tool forms)
  PASS D9 the originals changed right after the snapshot change nothing: A's ID and A's LIVE measurement
  PASS D10 the runtime identity is emitted from the verified snapshot, not from the release rewritten after it
  domain-release: all checks passed  (workdir /home/steven/enclave-bench/integration-tests-104622/domain-release)
  Unpinned verification is self-consistency only - a manifest rewritten to match a tampered file has it too (D3) -
  and is never presented as trust.

2. guestd-control/1 (m4/guestd/auth.go), unit and interop tests under -race
  --- PASS: TestWithoutCredentialsTheHandshakeRefusesSoAClientFailsClosed (0.00s)
  --- PASS: TestEveryRouteRefusesAnUnauthenticatedRequest (0.00s)
  --- PASS: TestTheHandshakeIsMutualAndAuthorisesSignedRequests (0.00s)
  --- PASS: TestTheHandshakeRefusesTheWrongKeyAndAnyReusedExpiredOrForeignNonce (0.01s)
  --- PASS: TestAReplayedOrOldRequestIsRefusedAndOrderWithinTheWindowIsNot (0.01s)
  --- PASS: TestATamperedRequestOrAnswerIsCaught (0.01s)
  --- PASS: TestNothingCrossesBetweenInstancesSharingAKey (0.00s)
  --- PASS: TestSessionsExpireAndARestartForcesAFreshHandshake (0.01s)
  --- PASS: TestTheTablesStayBoundedAndAreSwept (0.19s)
  --- PASS: TestAKeyFileMustBePrivateAndWellFormed (0.00s)
  --- PASS: TestTheJSClientInteroperates (0.10s)
  auth_test.go:453: interop: {"connect":"ok","health":200,"launch":"201 starting","lease":"200 [\"0xjs\"]","replay":"200 then 401","tampered":401,"reauth":"200 reconnected","otherKey":"THREW: guestd holds pairing key e41f9e2c433a1631, not ours (48d4b7ae6cfb7257)","labMode":"THREW: guestd at http://127.0.0.1:34705 does not speak guestd-control/1 (404: this guestd has no control credentials configured (unauthenticated lab mode)); refusing to use an unauthenticated manager","zeroKey":"THREW: the all-zero key is refused"}
  (all guestd tests: 32 PASS, 0 FAIL)

3. guestd-control/1 through the real binary's flags (test-guestd-control.sh)
  PASS C1 -gen-key writes a private key once and refuses to overwrite it (kid 9dfae13044e9c167)
  PASS C2 guestd refuses to start on a group- or world-readable key and on a malformed one
  PASS C3 with a key, every unauthenticated request is refused (401)
       client: {"health":200,"backend":"snp-guest-per-app","vms":0,"lease":200,"instance":"0a63ec10167557df0d40ae4552e514d5"}
  PASS C4 with a key, the supervisor's client handshakes with THIS instance and its signed requests are served
  PASS C5 without a key: lab mode, and the client refuses it rather than falling back
  PASS C6 a non-loopback listener is refused with and without a key
  guestd-control: all checks passed  (workdir /home/steven/enclave-bench/integration-tests-104622/control)

THE TRUST BOOTSTRAP, as built. One principal per guestd: whoever holds its pairing key (generated once, 0600, never
overwritten; refused if others can read it). The handshake is bound to a per-start instance id and a single-use
nonce, and guestd proves the key back. Every request is MAC'd over session, sequence, method, exact target and
body, with a replay window; every answer is signed. Sessions expire, and a restart ends them all.

WHAT THIS DOES NOT ESTABLISH
  - Delivery of the key into the node CVM, and the transport bridge (vsock, or slirp to loopback): not built. guestd
    still listens on loopback only, with or without a key (C6 above).
  - Protection from the host's root: the host runs guestd and its guests and can stop them regardless. The key
    stops other local users and processes, other VMs on a future bridge, replay and cross-instance confusion.
  - Any security property of a GUEST: the supervisor must still verify each guest's attestation itself; nothing a
    guest's security rests on is taken from this channel.
  - The supervisor using control-client.mjs: not wired.
  - Independent review.
