The runtime SET, admitted whole: the interpreter and shared libraries are now inside admission
warden-host, 2026-09-24. Run /home/steven/enclave-bench/m4b-rtset-083233, generated by m4/write-runtime-set-evidence.sh from files in that directory.
Guest code and encoder at 9d678395; the hardware harness at f3825d92, which changed no guest code, and whose files
are also copied with SHA256SUMS into /home/steven/enclave-bench/m4b-rtset-083233/harness/.
Local tests re-run at f3825d92. One plane, plane 2. NOT independently reviewed: the reviewer is unavailable, and nothing
after 39b5a5e4 has been reviewed.

WHAT WAS OPEN. The plane runs /rt/ld-linux-x86-64.so.2 --library-path /rt /rt/wasmtime, and the SVSM admitted the
wasmtime ELF alone, so the interpreter, libc, libm and libgcc_s executed without the SVSM ever hashing them. Both
earlier evidence files (step2-measured-boot, plane-handshake-binding) say so, and stay scoped to that.

WHAT CHANGED. The runtime kind now admits ONE canonical encoding of every file in /rt (guest/rtset.h,
"enclave-runtime-set-v1": each member's name, length and bytes, in bytewise name order; a subdirectory, symlink or
any other non-file refuses the whole set). ENCLAVE_RUNTIME_SHA256 is sha256 of that encoding. The SVSM's code did
not change: it hashes bytes and compares digests, so a changed, missing, added or renamed file is refused by the
same comparison as any other artifact. One encoder, compiled into the guest and into the host tool that prints the
digest, so the digest the SVSM is built for and the bytes the plane stages cannot come from two implementations.

THE BUILD (good)
  IGVM            /home/steven/enclave-bench/m4b-rtset-083233/good/plane.igvm
                  sha256 5fa14ec7279ece556bb869dfb6c6e090f672f8396af2c83acf97369cc013c2f8
  launch digest   CE3DB8CAC5DA7CD4018AA4A36D7BBB93C6054CBB1A272BB67A088F0A5E8435C25480D49CB7E20E7D0A743CCCEF637555
  firmware        OVMF.amdsev.debug.fd, sha256 50c063542f9886f8b3ec67ebe698ae1d617b7691bef89bb6eff4a34566930ec2
  svsm elf        81d92c43b7b733a918b47154301d9187c33c0f9e5858ae40ec8a13c3e919c626
  app id, plane 2 3458a52e477e3b658674e608af02678724bf578efed41f75690074c96242bd21
  runtime set     2c08b6b545b8e74131945bdb1ee74b926841817949c423de5bd97b3ad3f849f3
  runtime id      ccadb38a6779615597f0614311a631c70810916c1bbeb9f5706ee3a637fd90c8

THE GOOD PLANE: THE SET, AS THE GUEST STAGED IT AND AS THE SVSM FROZE IT
  PLANE runtime_member=ld-linux-x86-64.so.2 size=263152 elf=1
  PLANE runtime_member=libc.so.6 size=2215072 elf=1
  PLANE runtime_member=libgcc_s.so.1 size=182512 elf=1
  PLANE runtime_member=libm.so.6 size=1268336 elf=1
  PLANE runtime_member=runtime.json size=158 elf=0
  PLANE runtime_member=wasmtime size=45616736 elf=1
  PLANE runtime_set=members=6 elf=5 bytes=49546136 (rtset v1 over /rt, every entry)
  PLANE runtime=admitted
  PLANE status_after=admitted=0x03 required=0x03 kinds=2 vmpl=2 key=0 runtime_id=1
  PLANE whoami=3458a52e477e3b658674e608af02678724bf578efed41f75690074c96242bd21
and the SVSM's own console, which shares no code with the guest's lines:
  SVSM appid: plane 2 admitted kind 0, 15 pages frozen read-only to every guest plane
  SVSM appid: plane 2 admitted kind 1, 12097 pages frozen read-only to every guest plane
  SVSM appid: plane 2 registered a 91-byte transport key

12097 is ceil(49546136 / 4096): the SVSM hashed and froze the whole set. The wasmtime ELF alone was 11137 pages in
the handshake run, so a plane that staged the old artifact under the new label would show here.

WHAT THE RUNNING RUNTIME MAPPED, read from its own /proc maps before the front was started
  PLANE runtime_maps=ok elf_members_mapped=5/5 polls=2 outside_data=/app.wasm

Every executable file mapping is an admitted member - by path AND by the inode that was encoded - and every one of
the 5 admitted ELFs is mapped; the check waits for the second condition so it cannot pass by looking before the
loader finished. /app.wasm is the only other file mapping: read-only data, see the limits below.

THE GOOD PLANE STILL DOES EVERYTHING IT DID (verify-plane.sh, all ten checks)
  PASS 1 the plane's document is ATTESTED and the gate opens
  PASS 2 and one real application request was served over that connection
  PASS 3 the key in the document is the key of the handshake
  PASS 4 a verifier expecting VMPL0 REJECTS it
  PASS 5 a different app id REJECTS
  PASS 6 a different runtime identity REJECTS
  PASS 7 a different measurement REJECTS
  PASS 8 EVERY VMPCK (0-3) refused because that key is EMPTY, each in the kernel's own words
  PASS 9 the SVSM admitted the runtime SET (6 members listed by the guest, 49546136 bytes, 12097 pages frozen by the SVSM)
  PASS 10 the running runtime mapped every admitted ELF (5) and no executable file outside the set
  VERDICT attested
  RESULT report_vmpl=2
  RESULT gate=open
  RESULT app_requests_sent=1

THE NEGATIVE CONTROLS: A CHANGED FILE AND A MISSING FILE CANNOT INHERIT THE IDENTITY
Each image differs from the good one ONLY in /rt. Each was launched under an IGVM whose measured table covers THAT
image, so the firmware verified it and let it boot, and whose SVSM is the good run's own binary built for the GOOD
set. So the only thing that can refuse it is admission - which is the claim.

  changed   RT_MUTATE=flip:libc.so.6 (one byte, in the middle)
            this image's set 2b2440a0a7666d3706f9ddeedb738d43b613c6baf5ac2df821c8128147389068
            SVSM expects     2c08b6b545b8e74131945bdb1ee74b926841817949c423de5bd97b3ad3f849f3
            svsm elf         81d92c43b7b733a918b47154301d9187c33c0f9e5858ae40ec8a13c3e919c626
            launch digest    7BDA5DBDEC620A164757153B05F28CB7F44F0A91D275CCA4E2DCA9036C0884C4CB7067E1410B4C8549D68F8B4DF9FE4A
  Hash comparison succeeded for "kernel"
  Hash comparison succeeded for "initrd"
  Hash comparison succeeded for "cmdline"
  PLANE bundle=admitted
  PLANE runtime_set=members=6 elf=5 bytes=49546136 (rtset v1 over /rt, every entry)
  PLANE runtime=REFUSED: Permission denied
  PLANE refusal=ret=-22 rax_out=0x80001004
  PLANE serving=NO
  PLANE reason=the SVSM refused this plane's runtime set
  SVSM console:
  SVSM appid: plane 2 admitted kind 0, 15 pages frozen read-only to every guest plane

  missing   RT_MUTATE=drop:libgcc_s.so.1
            this image's set 06a2a365afa7d7d9166255d251596f92559c1736064cf42444c305828fb656c9
            SVSM expects     2c08b6b545b8e74131945bdb1ee74b926841817949c423de5bd97b3ad3f849f3
            svsm elf         81d92c43b7b733a918b47154301d9187c33c0f9e5858ae40ec8a13c3e919c626
            launch digest    FF7416C6D4AB251ABFFF2D58308EB5597DA78A02262BF43EEFDBE2E27850A3A3DB44D073E396563DF36E17029343C5F3
  Hash comparison succeeded for "kernel"
  Hash comparison succeeded for "initrd"
  Hash comparison succeeded for "cmdline"
  PLANE bundle=admitted
  PLANE runtime_set=members=5 elf=4 bytes=49363599 (rtset v1 over /rt, every entry)
  PLANE runtime=REFUSED: Permission denied
  PLANE refusal=ret=-22 rax_out=0x80001004
  PLANE serving=NO
  PLANE reason=the SVSM refused this plane's runtime set
  SVSM console:
  SVSM appid: plane 2 admitted kind 0, 15 pages frozen read-only to every guest plane

0x80001004 is E_DIGEST_MISMATCH, which appid.rs returns only after hashing the staged bytes and finding them unequal
to the compiled-in digest. The SVSM logged kind 0 (the bundle) and NO kind 1 in either negative: nothing of the set
was frozen, no whoami, no key registered, no document served; planeinit powered the domain off. The launch digests
also differ from the good plane's, so a verifier pinning the good one rejects these guests before admission is
reached - the negatives isolate the second layer by construction.

THE HARNESS'S OWN SCORE (run.log)
  PASS G the good plane passed every verify-plane.sh check (rc=0)
  PASS changed0 the image's set differs from the good one, and its SVSM is the good run's own binary built for the good set
  PASS changed1 its launch digest differs from the good plane's, so a verifier pinning that one rejects it first
  PASS changed2 the firmware verified all three blobs and the bundle was admitted, so the plane reached its runtime
  PASS changed3 the SVSM REFUSED the runtime set with 0x80001004, the digest-mismatch code - not some other refusal
  PASS changed4 and the plane was never named, never registered a key and never served: it powered off
  PASS missing0 the image's set differs from the good one, and its SVSM is the good run's own binary built for the good set
  PASS missing1 its launch digest differs from the good plane's, so a verifier pinning that one rejects it first
  PASS missing2 the firmware verified all three blobs and the bundle was admitted, so the plane reached its runtime
  PASS missing3 the SVSM REFUSED the runtime set with 0x80001004, the digest-mismatch code - not some other refusal
  PASS missing4 and the plane was never named, never registered a key and never served: it powered off
  M4b-runtime-set: all checks passed
  exit=0

LOCAL TESTS AT THE SAME CODE (no guest), test-rtset.out and svsm-unit-tests.out
  PASS 1 the set is exactly the runtime, its interpreter, every library ldd resolves for it, and runtime.json (6 members)
  PASS 2 the digest is reproducible, and independent of creation order, modes and timestamps
  PASS 3 an independent reading of the documented format gives the same digest
  PASS 4 ONE flipped bit in ANY member changes the digest (6 of 6 members)
  PASS 5 ANY member one byte shorter changes the digest (6 of 6)
  PASS 6 ANY member MISSING changes the digest (6 of 6), and restoring them all restores it
  PASS 7 an ADDED file and a RENAMED member each change the digest
  PASS 8 a glibc-hwcaps subdirectory, a symlink, a FIFO, an empty set and an oversized set are each REFUSED, not skipped
  PASS 9 the old meaning (sha256 of the wasmtime ELF) is not the set digest, so an SVSM built for either refuses the other
  PASS 10 the plane's own command line maps EVERY admitted ELF (5) and no executable outside the set
  PASS 11 the maps check REFUSES an unadmitted executable, a member replaced after admission, and a load it never saw complete
  PASS 12 the plane image's /rt encodes to the digest its build printed, which is this host's set
  PASS 13 the admission fixture carries the same set under the same digest: one meaning of ENCLAVE_RUNTIME_SHA256
  PASS 14 a negative-control build (one byte of libc) prints a DIFFERENT digest and says it is a negative control
  rtset: all checks passed  (workdir /home/steven/enclave-bench/m4b-rtset-083233/local-test)
  SVSM appid unit tests: ok. 44 passed; 0 failed; 0 ignored; 0 measured; 159 filtered out; finished in 0.00s

Among them: EVERY member of the set (6 of 6), not a sample, changes the digest when one bit flips, when it is one
byte short, and when it is absent; an added and a renamed file change it; a glibc-hwcaps subdirectory, a symlink,
a FIFO, an empty set and an oversized set are refused rather than skipped; and the maps check refuses a process
that maps an unadmitted executable, one whose member was renamed over after encoding (same bytes, new inode), and
one that never finished loading the set.

WHAT THIS DOES NOT ESTABLISH

  ONE app on ONE plane. Nothing here demonstrates isolation BETWEEN two apps on two planes, which is the
  requirement. The second-plane blocker stands unchanged: precondition 4's mechanism is unresolved (see
  HOST-CHANGE-PLAN.md), and no run is to be scored as isolation.

  THE COMPONENT IS NOT ADMITTED. What the runtime compiles and runs is /app.wasm, extracted from the bundle at
  BUILD time (build-plane-guest.sh). The admitted artifact is the bundle. Both are in the initrd the firmware
  verifies under step 2, so the component is inside the launch measurement - but the link between the admitted
  bundle and the executed component is the build, not admission. That is the next gap of the same shape; closing
  it means planeinit handing the runtime the component cut from the file it admitted.

  THE MAPS CHECK IS ONE READING AT LOAD. It sees what the loader mapped at start. A library opened later with
  dlopen would not be seen. None was observed for this command line on the host (LD_DEBUG=libs,files, one request
  served), which is an observation about this wasmtime build, not a guarantee.

  THE CHECK IS THE PLANE'S OWN WORD. planeinit performs it and prints it; the hardware does not attest it. It is
  worth relying on because planeinit is inside the initrd the launch measurement covers under step 2 and it fails
  closed (powers off) - the same standing as the VMPCK probe, and no stronger.

  THE FROZEN PAGES ARE A COPY. The SVSM hashes and freezes the STAGED copy of the set; the runtime executes the
  files in /rt. What ties them is that planeinit reads the same files it later starts, and that the maps check
  requires the executing file objects to be the ones it encoded (same inode). An in-place write to an /rt file
  between admission and start would need a writer inside the plane, and only measured code runs there; this is
  not enforced by hardware.

  THE LIBRARY LIST COMES FROM ldd ON THE BUILD HOST. The maps check is what guards it at runtime: a library the list
  missed would be an unadmitted executable mapping, and a listed one nothing loads would never be mapped - both stop
  the plane.

  THE STEP-2 FIXTURE WAS NOT RE-RUN. admitinit now stages the set as well (one meaning of ENCLAVE_RUNTIME_SHA256), and
  test-rtset check 13 shows its image carries the same set under the same digest, but verify-measured-boot.sh was
  not re-run on hardware. evidence/step2-measured-boot-2026-09-24.txt remains the record of the ELF-only fixture at
  its own revision.

  NOT INDEPENDENTLY REVIEWED.
