{
  "mode": "snp",
  "name": "metal0",
  "cpus": 8,
  "memMiB": 8192,

  "publicUrl": "https://metal0.enclave.host",
  "relayUrl": "wss://api.enclave.host/v1/fleet-tunnel",
  "tunnelToken": "REPLACE_WITH_A_SHARED_TUNNEL_TOKEN",

  "hostfwd": [
    { "host": 18080, "guest": 8080 }
  ],

  "ovmf": "/usr/share/edk2/x64/OVMF.4m.fd",
  "sevDevice": "/dev/sev",
  "qemu": "qemu-system-x86_64",
  "dist": "metal/dist",

  "_comment": "mode: snp | tdx | dev. dev = plain KVM (no attestation) for bring-up. hostfwd exposes guest ports on host loopback for local testing; production reachability is the fleet tunnel (relayUrl), so hostfwd can be empty. tunnelToken must match the relay's metal allowlist entry for this name."
}
