{
  "name": "nan-relay",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "description": "Untrusted public TCP relay for Enclave enclaves — SNI-routes client TLS to the enclave's /x/:id/tls/:port bridge. Holds no keys, sees only ciphertext.",
  "main": "relay.js",
  "scripts": {
    "start": "node relay.js",
    "start:api": "node api-relay.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1",
    "jose": "^5.9.6",
    "viem": "^2.53.1",
    "ws": "^8.21.0"
  }
}
