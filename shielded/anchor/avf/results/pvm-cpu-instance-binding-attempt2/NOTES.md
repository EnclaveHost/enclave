# Instance binding, device attempt 2 (kept as it is)

STOPPED at A (19:58Z): enrollment worked (real v3, Google roots: ccd79db1..., the VM's logged id); the bound turn went 1 s later and the VM answered its pace refusal (evidence/evidence-002.json: {"error":"one evidence answer every 2 s"}). The client refused (fail closed) but named it a downgrade; it now names the VM's error, and the script waits 3 s before every exchange.

The passing run and all four attempts are described in ../pvm-cpu-instance-binding/NOTES.md.
