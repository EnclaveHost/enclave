/* ============================================================
   Privacy Policy - static legal copy. The page module exists
   so the router can soft-navigate here and the shared chrome
   (header wallet button, toast) hydrates; there is nothing else
   to boot. Mirrors pages/terms.js.
   ============================================================ */
import "../../components/header/header.js";
import "../../components/footer/footer.js";
import "../../components/toast/toast.js";
import "../../components/section-head/section-head.js";

export function boot(){}
