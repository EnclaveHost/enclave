# admission-vectors.json

A copy of `verifier/admission-vectors.json` from the Enclave verifier session's branch `research/independent-verifier`,
commit 68250600, unchanged at 88c97af0 (49 cases, with the v3 instance-binding cases; before them, d760725b's 40) (generated there by `verifier/admission-vectors.mjs` from `verifier/admission.mjs`, and replayed there by
`test/verifier-admission-vectors.test.mjs`). Data only; no code is copied. test/pvm-client-gate.test.mjs holds this
client's release rule (shielded/anchor/avf/client/src/gate.js) to it. Refresh by copying the file from a newer commit and
updating this note; never edit it by hand.
