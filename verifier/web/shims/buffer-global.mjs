// Injected by esbuild (verifier/web/build.mjs): the `buffer` package's Buffer stands in for Node's, so the shared modules'
// Buffer uses (subarray, equals, from, alloc) run unchanged in a page.
import { Buffer } from "buffer";
export { Buffer };
