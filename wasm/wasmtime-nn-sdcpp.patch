diff --git a/crates/wasi-nn/Cargo.toml b/crates/wasi-nn/Cargo.toml
index 48e79b20f3..f6edc2aa46 100644
--- a/crates/wasi-nn/Cargo.toml
+++ b/crates/wasi-nn/Cargo.toml
@@ -73,6 +73,10 @@ onnx = ["dep:ort"]
 # ggml/llama.cpp backend: pure FFI against the system-linked enclave_llama
 # shim (ELL_LIB_LOCATION at build time) - no cargo dependencies at all.
 ggml = []
+# stable-diffusion.cpp backend: pure FFI against the system-linked enclave_sd
+# shim (ESD_LIB_LOCATION at build time) - no cargo dependencies at all.
+# Preload-only ("-S nn-graph=sd::<dir>" + load_by_name).
+sdcpp = []
 # Use prebuilt ONNX Runtime binaries from ort.
 onnx-download = ["onnx", "ort/download-binaries"]
 # CUDA execution provider for NVIDIA GPU support (requires CUDA toolkit)
diff --git a/crates/wasi-nn/build.rs b/crates/wasi-nn/build.rs
index af89d103d8..cac6b1ee9a 100644
--- a/crates/wasi-nn/build.rs
+++ b/crates/wasi-nn/build.rs
@@ -12,6 +12,17 @@ fn main() {
         println!("cargo:rustc-link-lib=dylib=llama");
     }
 
+    // sdcpp backend links the enclave_sd shim (and transitively
+    // libstable-diffusion) from ESD_LIB_LOCATION - same pattern.
+    if std::env::var("CARGO_FEATURE_SDCPP").is_ok() {
+        println!("cargo:rerun-if-env-changed=ESD_LIB_LOCATION");
+        if let Ok(dir) = std::env::var("ESD_LIB_LOCATION") {
+            println!("cargo:rustc-link-search=native={dir}");
+        }
+        println!("cargo:rustc-link-lib=dylib=enclave_sd");
+        println!("cargo:rustc-link-lib=dylib=stable-diffusion");
+    }
+
     let cwd = std::env::current_dir().unwrap();
     let wasi_root = cwd.join("witx");
 
diff --git a/crates/wasi-nn/src/backend/mod.rs b/crates/wasi-nn/src/backend/mod.rs
index b8d871003e..05697c5ed1 100644
--- a/crates/wasi-nn/src/backend/mod.rs
+++ b/crates/wasi-nn/src/backend/mod.rs
@@ -6,6 +6,8 @@
 pub mod ggml;
 #[cfg(feature = "onnx")]
 pub mod onnx;
+#[cfg(feature = "sdcpp")]
+pub mod sdcpp;
 #[cfg(all(feature = "openvino", target_pointer_width = "64"))]
 pub mod openvino;
 #[cfg(feature = "pytorch")]
@@ -17,6 +19,8 @@ pub mod winml;
 use self::ggml::GgmlBackend;
 #[cfg(feature = "onnx")]
 use self::onnx::OnnxBackend;
+#[cfg(feature = "sdcpp")]
+use self::sdcpp::SdCppBackend;
 #[cfg(all(feature = "openvino", target_pointer_width = "64"))]
 use self::openvino::OpenvinoBackend;
 #[cfg(feature = "pytorch")]
@@ -52,6 +56,10 @@ pub fn list() -> Vec<Backend> {
     {
         backends.push(Backend::from(GgmlBackend::default()));
     }
+    #[cfg(feature = "sdcpp")]
+    {
+        backends.push(Backend::from(SdCppBackend::default()));
+    }
     #[cfg(feature = "pytorch")]
     {
         backends.push(Backend::from(PytorchBackend::default()));
@@ -62,6 +70,14 @@ pub fn list() -> Vec<Backend> {
 /// A [Backend] contains the necessary state to load [Graph]s.
 pub trait BackendInner: Send + Sync {
     fn encoding(&self) -> GraphEncoding;
+    /// Enclave: the `-S nn-graph=<kind>::<dir>` kind string this backend
+    /// answers to, for PRELOAD-ONLY backends whose kind is not a
+    /// guest-visible GraphEncoding case (adding enum cases would change the
+    /// component-level type and break instantiation of every published
+    /// app). Backends reachable through an encoding leave this empty.
+    fn preload_kind(&self) -> &'static str {
+        ""
+    }
     fn load(&mut self, builders: &[&[u8]], target: ExecutionTarget) -> Result<Graph, BackendError>;
     fn as_dir_loadable<'a>(&'a mut self) -> Option<&'a mut dyn BackendFromDir>;
 }
diff --git a/crates/wasi-nn/src/backend/sdcpp.rs b/crates/wasi-nn/src/backend/sdcpp.rs
new file mode 100644
index 0000000..cf88385
--- /dev/null
+++ b/crates/wasi-nn/src/backend/sdcpp.rs
@@ -0,0 +1,742 @@
+//! A stable-diffusion.cpp backend for wasi-nn — image generation models
+//! (safetensors/GGUF checkpoints) behind the standard `wasi:nn` interface.
+//!
+//! The heavy lifting happens in the system-linked `enclave_sd` shim (a flat
+//! pointer/scalar C ABI over stable-diffusion.cpp, shipped next to
+//! libstable-diffusion in the toolchain artifact — see wasm/sd-shim in the
+//! platform repo). Same discipline as the ggml/llama backend: std + FFI
+//! only, no new crates.
+//!
+//! Model delivery is `load_by_name()` via the CLI preload registry
+//! (`-S nn-graph=sd::<dir>`): the HOST opens the checkpoint straight off the
+//! (read-only, attested) model volume at server startup, so weights never
+//! transit the guest's 32-bit linear memory — image checkpoints (SD-Turbo
+//! fp32 5.2 GB, SDXL, FLUX quants) routinely exceed both the guest's 4 GiB
+//! address space and the per-request hostcall-fuel copy budget, which is the
+//! point of this backend. `load()` from guest bytes is deliberately
+//! unsupported. The backend is preload-only and never claims a guest-visible
+//! GraphEncoding case (adding one would change the component-level enum type
+//! and break instantiation of every already-published app; `encoding()`
+//! returns the otherwise-unused Autodetect variant).
+//!
+//! Inference contract (WIT named tensors) — one whole txt2img pipeline per
+//! `compute()`, blocking until the image is ready:
+//!   input  "prompt"           U8   [1, len]  utf-8 (required)
+//!   input  "negative_prompt"  U8   [1, len]  utf-8 (optional)
+//!   input  "steps"            I32  [1]       (default 20)
+//!   input  "seed"             I64  [1]       (default 42)
+//!   input  "width"/"height"   I32  [1]       (default 512, multiples of 64)
+//!   input  "cfg"              Fp32 [1]       (default 7.0; 1.0 = off, the
+//!                                             turbo/distilled setting)
+//!   input  "sample_method"    U8   [1, len]  sd.cpp name ("euler_a", ...;
+//!                                             default: the model's default)
+//!   input  "scheduler"        U8   [1, len]  sd.cpp name ("discrete", ...)
+//!   output "image"            U8   [1, h, w, 3]  RGB row-major
+//!
+//! UPSCALER volumes: a volume carrying the file named by
+//! ENCLAVE_SD_UPSCALE_FILE preloads as an ESRGAN super-resolution graph
+//! (RRDBNet family — Real-ESRGAN and finetunes; scale 1/2/4 read from the
+//! weights) instead of a generation pipeline, through sd.cpp's second
+//! engine. Same load_by_name delivery, one upscale per `compute()`:
+//!   input  "image"            U8   [1, h, w, 3]  RGB row-major
+//!   output "image"            U8   [1, h*s, w*s, 3]  (s = the model scale)
+//! The shim entry points behind this are dlsym-OPTIONAL (same doctrine as
+//! the ggml backend's vision verbs): a toolchain tarball predating them
+//! still serves txt2img, upscaler volumes just fail preload with a
+//! "rebuild the toolchain" error.
+//!
+//! Tuning env (host process only, set per tenant by the wasm-manager):
+//!   ENCLAVE_SD_USE_GPU     1 (default) = weights on the GPU, and a missing
+//!                          GPU device is a LOUD preload failure (tenants buy
+//!                          the share - same discipline as ggml/ONNX-strict);
+//!                          0 = pure CPU (dev boxes)
+//!   ENCLAVE_SD_WTYPE       weight type conversion on load ("" = keep source;
+//!                          "f16" halves an f32 checkpoint; quant names work)
+//!   ENCLAVE_SD_N_THREADS   CPU threads (default: all physical cores)
+//!   ENCLAVE_SD_FLASH_ATTN  1 = enable flash attention on the diffusion model
+//!   ENCLAVE_SD_VAE_TILING  1 = decode the VAE in tiles: caps the decode
+//!                          buffer at ~O(tile) instead of O(image) - a ~6 GB
+//!                          spike at 1024px becomes <1 GB - at the cost of
+//!                          tile-blend seam risk. Deployment knob, default 0.
+//!   ENCLAVE_SD_MODEL_FILE  checkpoint inside the volume dir, for dirs where
+//!                          the single-file convention below is ambiguous
+//!   ENCLAVE_SD_DIFFUSION_FILE / _CLIP_L_FILE / _CLIP_G_FILE / _T5XXL_FILE /
+//!   _VAE_FILE              component-file mode (FLUX-style split models),
+//!                          all relative to the volume dir
+//!   ENCLAVE_SD_LLM_FILE    the LLM text encoder for 2025+ DiT families
+//!                          (Qwen-Image: Qwen2.5-VL; Z-Image: Qwen3-4B;
+//!                          sd.cpp's --llm), relative to the volume dir;
+//!                          combines with either mode above
+//!   ENCLAVE_SD_UPSCALE_FILE  the upscale model inside an UPSCALER volume.
+//!                          A DETECTOR, not a validator: the env is
+//!                          node-global like the component vars, but names a
+//!                          file only upscaler volumes carry — present, the
+//!                          volume preloads as an upscale graph and the
+//!                          component/checkpoint rules don't apply; absent,
+//!                          the volume is a generation volume as before.
+//!   ENCLAVE_SD_UPSCALE_TILE  upscale input tile size, px (default 256; 0 =
+//!                          whole image in one graph). Peak activation
+//!                          memory is O(tile^2) instead of O(image) — the
+//!                          knob that lets a 2048px input upscale inside a
+//!                          GPU share. Tiles blend with 0.25 overlap.
+//!
+//! Single-file convention (no env): model.gguf, else model.safetensors, else
+//! exactly one top-level *.safetensors or *.gguf in the dir.
+
+use super::{
+    BackendError, BackendExecutionContext, BackendFromDir, BackendGraph, BackendInner, Id,
+    NamedTensor,
+};
+use crate::wit::types::{ExecutionTarget, GraphEncoding, Tensor, TensorType};
+use crate::{ExecutionContext, Graph};
+use std::ffi::{c_char, c_void, CStr, CString};
+use std::path::{Path, PathBuf};
+use std::sync::{Arc, Mutex, Once, OnceLock};
+
+unsafe extern "C" {
+    fn esd_init();
+    fn esd_gpu_devices() -> i32;
+    fn esd_load_model(
+        model_path: *const c_char,
+        diffusion_path: *const c_char,
+        clip_l_path: *const c_char,
+        clip_g_path: *const c_char,
+        t5xxl_path: *const c_char,
+        llm_path: *const c_char,
+        vae_path: *const c_char,
+        n_threads: i32,
+        wtype: *const c_char,
+        use_gpu: i32,
+        flash_attn: i32,
+        vae_tiling: i32,
+    ) -> *mut c_void;
+    fn esd_free_model(handle: *mut c_void);
+    fn esd_txt2img(
+        handle: *mut c_void,
+        prompt: *const c_char,
+        negative_prompt: *const c_char,
+        width: i32,
+        height: i32,
+        steps: i32,
+        cfg: f32,
+        seed: i64,
+        sample_method: *const c_char,
+        scheduler: *const c_char,
+        rgb_out: *mut u8,
+    ) -> i32;
+    fn esd_last_error() -> *const c_char;
+}
+
+// dlsym, for the OPTIONAL upscaler entry points below (in libc since glibc
+// 2.34 — no extra link dependency).
+unsafe extern "C" {
+    fn dlsym(handle: *mut c_void, symbol: *const c_char) -> *mut c_void;
+}
+
+type EsdLoadUpscalerFn = unsafe extern "C" fn(*const c_char, i32, i32, i32) -> *mut c_void;
+type EsdFreeUpscalerFn = unsafe extern "C" fn(*mut c_void);
+type EsdUpscalerScaleFn = unsafe extern "C" fn(*mut c_void) -> i32;
+type EsdUpscaleFn = unsafe extern "C" fn(
+    *mut c_void, // handle
+    *const u8,   // rgb in, width*height*3
+    i32,         // width
+    i32,         // height
+    *mut u8,     // rgb out, (width*scale)*(height*scale)*3
+) -> i32;
+
+/// The upscaler entry points, resolved by dlsym instead of linked (same
+/// doctrine as the ggml backend's vision verbs): a toolchain tarball whose
+/// libenclave_sd predates them still builds, links and serves txt2img —
+/// upscaler volumes then fail preload with a "rebuild the toolchain" error
+/// instead of taking the whole node down.
+struct UpscalerSyms {
+    load: EsdLoadUpscalerFn,
+    free: EsdFreeUpscalerFn,
+    scale: EsdUpscalerScaleFn,
+    upscale: EsdUpscaleFn,
+}
+
+static UPSCALER_SYMS: OnceLock<Option<UpscalerSyms>> = OnceLock::new();
+
+fn upscaler_syms() -> Option<&'static UpscalerSyms> {
+    UPSCALER_SYMS
+        .get_or_init(|| {
+            let sym = |c: &CStr| unsafe { dlsym(std::ptr::null_mut(), c.as_ptr()) };
+            let (load, free, scale, upscale) = (
+                sym(c"esd_load_upscaler"),
+                sym(c"esd_free_upscaler"),
+                sym(c"esd_upscaler_scale"),
+                sym(c"esd_upscale"),
+            );
+            if load.is_null() || free.is_null() || scale.is_null() || upscale.is_null() {
+                return None;
+            }
+            unsafe {
+                Some(UpscalerSyms {
+                    load: std::mem::transmute::<*mut c_void, EsdLoadUpscalerFn>(load),
+                    free: std::mem::transmute::<*mut c_void, EsdFreeUpscalerFn>(free),
+                    scale: std::mem::transmute::<*mut c_void, EsdUpscalerScaleFn>(scale),
+                    upscale: std::mem::transmute::<*mut c_void, EsdUpscaleFn>(upscale),
+                })
+            }
+        })
+        .as_ref()
+}
+
+static INIT: Once = Once::new();
+
+fn err(msg: impl Into<String>) -> BackendError {
+    BackendError::BackendAccess(wasmtime::format_err!("{}", msg.into()))
+}
+
+fn last_error() -> String {
+    let s = unsafe { esd_last_error() };
+    if s.is_null() {
+        return String::new();
+    }
+    unsafe { CStr::from_ptr(s) }.to_string_lossy().into_owned()
+}
+
+fn env_flag(name: &str, default: bool) -> bool {
+    match std::env::var(name) {
+        Ok(v) => !matches!(v.trim(), "0" | "false" | "off" | ""),
+        Err(_) => default,
+    }
+}
+
+/// A file named by env `var`, resolved inside `dir` (absolute paths and `..`
+/// are refused - the volume is the trust boundary).
+fn env_file(dir: &Path, var: &str) -> Result<Option<PathBuf>, BackendError> {
+    let Ok(rel) = std::env::var(var) else { return Ok(None) };
+    let rel = rel.trim();
+    if rel.is_empty() {
+        return Ok(None);
+    }
+    if rel.starts_with('/') || rel.split('/').any(|c| c == "..") {
+        return Err(err(format!("{var} must be a relative path inside the volume, got '{rel}'")));
+    }
+    let p = dir.join(rel);
+    if !p.is_file() {
+        return Err(err(format!("{var}={rel} does not exist in {}", dir.display())));
+    }
+    Ok(Some(p))
+}
+
+/// Like `env_file`, but a DETECTOR: a set-but-absent file means "not this
+/// kind of volume" (None), never an error — the env is node-global and most
+/// volumes legitimately lack the file. Path hygiene still enforced.
+fn env_file_detect(dir: &Path, var: &str) -> Result<Option<PathBuf>, BackendError> {
+    let Ok(rel) = std::env::var(var) else { return Ok(None) };
+    let rel = rel.trim();
+    if rel.is_empty() {
+        return Ok(None);
+    }
+    if rel.starts_with('/') || rel.split('/').any(|c| c == "..") {
+        return Err(err(format!("{var} must be a relative path inside the volume, got '{rel}'")));
+    }
+    let p = dir.join(rel);
+    Ok(p.is_file().then_some(p))
+}
+
+/// The single-file checkpoint in `dir` per the convention in the module doc.
+fn pick_checkpoint(dir: &Path) -> Result<PathBuf, BackendError> {
+    for name in ["model.gguf", "model.safetensors"] {
+        let p = dir.join(name);
+        if p.is_file() {
+            return Ok(p);
+        }
+    }
+    let mut candidates: Vec<PathBuf> = std::fs::read_dir(dir)
+        .map_err(|e| err(format!("reading {}: {e}", dir.display())))?
+        .filter_map(|e| e.ok().map(|e| e.path()))
+        .filter(|p| {
+            p.is_file()
+                && p.extension()
+                    .map(|x| x == "safetensors" || x == "gguf" || x == "ckpt")
+                    .unwrap_or(false)
+        })
+        .collect();
+    match (candidates.len(), candidates.pop()) {
+        (1, Some(p)) => Ok(p),
+        (n, _) => Err(err(format!(
+            "expected model.gguf, model.safetensors, or exactly one top-level \
+             *.safetensors/*.gguf in {} (found {n}); set ENCLAVE_SD_MODEL_FILE \
+             or the ENCLAVE_SD_*_FILE component vars to disambiguate",
+            dir.display()
+        ))),
+    }
+}
+
+fn cstring(p: &Path) -> Result<CString, BackendError> {
+    CString::new(p.to_string_lossy().as_bytes()).map_err(|_| err("path contains NUL"))
+}
+
+fn opt_ptr(c: &Option<CString>) -> *const c_char {
+    c.as_ref().map_or(std::ptr::null(), |s| s.as_ptr())
+}
+
+#[derive(Default)]
+pub struct SdCppBackend();
+unsafe impl Send for SdCppBackend {}
+unsafe impl Sync for SdCppBackend {}
+
+impl BackendInner for SdCppBackend {
+    fn encoding(&self) -> GraphEncoding {
+        // Preload-only backend: reachable via `-S nn-graph=sd::<dir>` +
+        // load_by_name. Autodetect is unclaimed by real backends, so guest
+        // load() calls can never accidentally land here with model bytes.
+        GraphEncoding::Autodetect
+    }
+
+    fn preload_kind(&self) -> &'static str {
+        "sd"
+    }
+
+    fn load(&mut self, _builders: &[&[u8]], _target: ExecutionTarget) -> Result<Graph, BackendError> {
+        Err(err(
+            "the sd backend does not load from guest bytes - image checkpoints exceed \
+             guest memory; attach the model volume and use load_by_name",
+        ))
+    }
+
+    fn as_dir_loadable<'a>(&'a mut self) -> Option<&'a mut dyn BackendFromDir> {
+        Some(self)
+    }
+}
+
+impl BackendFromDir for SdCppBackend {
+    fn load_from_dir(&mut self, dir: &Path, _target: ExecutionTarget) -> Result<Graph, BackendError> {
+        INIT.call_once(|| unsafe { esd_init() });
+
+        // Registry preloads arrive with ExecutionTarget::Cpu; the env decides
+        // real placement (same pattern as the ggml backend). Strict GPU: the
+        // CUDA path missing is a loud startup failure, never a silent CPU
+        // fallback at tenant speed.
+        let use_gpu = env_flag("ENCLAVE_SD_USE_GPU", true);
+        if use_gpu && unsafe { esd_gpu_devices() } == 0 {
+            return Err(err(
+                "sd: GPU requested (ENCLAVE_SD_USE_GPU != 0) but no ggml GPU device is \
+                 available - CUDA backend module or driver missing",
+            ));
+        }
+
+        // Upscaler volume? Decided FIRST: an upscaler volume carries exactly
+        // one model file, which the single-checkpoint convention below would
+        // otherwise happily misload as a generation checkpoint.
+        if let Some(upscale_model) = env_file_detect(dir, "ENCLAVE_SD_UPSCALE_FILE")? {
+            return load_upscaler(dir, &upscale_model, use_gpu);
+        }
+
+        let diffusion = env_file(dir, "ENCLAVE_SD_DIFFUSION_FILE")?;
+        let (model, components) = if diffusion.is_some() {
+            (
+                None,
+                (
+                    diffusion,
+                    env_file(dir, "ENCLAVE_SD_CLIP_L_FILE")?,
+                    env_file(dir, "ENCLAVE_SD_CLIP_G_FILE")?,
+                    env_file(dir, "ENCLAVE_SD_T5XXL_FILE")?,
+                    env_file(dir, "ENCLAVE_SD_VAE_FILE")?,
+                ),
+            )
+        } else {
+            let model = match env_file(dir, "ENCLAVE_SD_MODEL_FILE")? {
+                Some(p) => p,
+                None => pick_checkpoint(dir)?,
+            };
+            (Some(model), (None, None, None, None, None))
+        };
+        let (diffusion, clip_l, clip_g, t5xxl, vae) = components;
+        // the LLM text encoder rides alongside EITHER mode: split-component
+        // layouts (Qwen-Image, Z-Image) and single-checkpoint volumes that
+        // ship the encoder as a separate gguf both occur in the wild
+        let llm = env_file(dir, "ENCLAVE_SD_LLM_FILE")?;
+
+        let model_c = model.as_deref().map(cstring).transpose()?;
+        let diffusion_c = diffusion.as_deref().map(cstring).transpose()?;
+        let clip_l_c = clip_l.as_deref().map(cstring).transpose()?;
+        let clip_g_c = clip_g.as_deref().map(cstring).transpose()?;
+        let t5xxl_c = t5xxl.as_deref().map(cstring).transpose()?;
+        let llm_c = llm.as_deref().map(cstring).transpose()?;
+        let vae_c = vae.as_deref().map(cstring).transpose()?;
+        let wtype = CString::new(std::env::var("ENCLAVE_SD_WTYPE").unwrap_or_default())
+            .map_err(|_| err("ENCLAVE_SD_WTYPE contains NUL"))?;
+        let n_threads: i32 = std::env::var("ENCLAVE_SD_N_THREADS")
+            .ok()
+            .and_then(|v| v.parse().ok())
+            .unwrap_or(0);
+
+        let handle = unsafe {
+            esd_load_model(
+                opt_ptr(&model_c),
+                opt_ptr(&diffusion_c),
+                opt_ptr(&clip_l_c),
+                opt_ptr(&clip_g_c),
+                opt_ptr(&t5xxl_c),
+                opt_ptr(&llm_c),
+                opt_ptr(&vae_c),
+                n_threads,
+                wtype.as_ptr(),
+                use_gpu as i32,
+                env_flag("ENCLAVE_SD_FLASH_ATTN", false) as i32,
+                env_flag("ENCLAVE_SD_VAE_TILING", false) as i32,
+            )
+        };
+        if handle.is_null() {
+            return Err(err(format!(
+                "stable-diffusion.cpp failed to load {}: {}",
+                model.or(diffusion).unwrap_or_else(|| dir.to_path_buf()).display(),
+                last_error()
+            )));
+        }
+        let graph: Box<dyn BackendGraph> = Box::new(SdGraph(Arc::new(SdModel {
+            raw: Mutex::new(handle as usize),
+        })));
+        Ok(graph.into())
+    }
+}
+
+/// The loaded sd_ctx. generate_image is not documented thread-safe, so the
+/// raw handle lives behind a Mutex - one generation at a time per model
+/// (requests queue; an image is seconds, not minutes, on the GPU).
+struct SdModel {
+    raw: Mutex<usize>,
+}
+impl Drop for SdModel {
+    fn drop(&mut self) {
+        let raw = *self.raw.lock().unwrap();
+        unsafe { esd_free_model(raw as *mut c_void) };
+    }
+}
+
+struct SdGraph(Arc<SdModel>);
+
+impl BackendGraph for SdGraph {
+    fn init_execution_context(&self) -> Result<ExecutionContext, BackendError> {
+        let ctx: Box<dyn BackendExecutionContext> = Box::new(SdExecutionContext {
+            model: self.0.clone(),
+            image: None,
+        });
+        Ok(ctx.into())
+    }
+}
+
+struct SdExecutionContext {
+    model: Arc<SdModel>,
+    image: Option<Tensor>, // last compute's output for get_output
+}
+
+fn utf8_input(nt: &NamedTensor) -> Result<String, BackendError> {
+    if nt.tensor.ty != TensorType::U8 {
+        return Err(BackendError::UnsupportedTensorType(format!(
+            "{:?} (input \"{}\" wants U8 utf-8 bytes)",
+            nt.tensor.ty, nt.name
+        )));
+    }
+    String::from_utf8(nt.tensor.data.clone())
+        .map_err(|_| err(format!("input \"{}\" is not valid utf-8", nt.name)))
+}
+
+fn scalar_i64(nt: &NamedTensor) -> Result<i64, BackendError> {
+    let d = &nt.tensor.data;
+    match (nt.tensor.ty, d.len()) {
+        (TensorType::I32, 4) => Ok(i32::from_le_bytes([d[0], d[1], d[2], d[3]]) as i64),
+        (TensorType::I64, 8) => Ok(i64::from_le_bytes(d[..8].try_into().unwrap())),
+        _ => Err(err(format!(
+            "input \"{}\" wants a single I32/I64 scalar, got {:?} x{} bytes",
+            nt.name, nt.tensor.ty, d.len()
+        ))),
+    }
+}
+
+fn scalar_f32(nt: &NamedTensor) -> Result<f32, BackendError> {
+    let d = &nt.tensor.data;
+    match (nt.tensor.ty, d.len()) {
+        (TensorType::Fp32, 4) => Ok(f32::from_le_bytes([d[0], d[1], d[2], d[3]])),
+        _ => Err(err(format!(
+            "input \"{}\" wants a single Fp32 scalar, got {:?} x{} bytes",
+            nt.name, nt.tensor.ty, d.len()
+        ))),
+    }
+}
+
+impl SdExecutionContext {
+    fn generate(&mut self, inputs: &[NamedTensor]) -> Result<Tensor, BackendError> {
+        let mut prompt = None;
+        let mut negative = String::new();
+        let mut sample_method = String::new();
+        let mut scheduler = String::new();
+        let (mut steps, mut seed) = (20i64, 42i64);
+        let (mut width, mut height) = (512i64, 512i64);
+        let mut cfg = 7.0f32;
+        for nt in inputs {
+            match nt.name.as_str() {
+                "prompt" => prompt = Some(utf8_input(nt)?),
+                "negative_prompt" => negative = utf8_input(nt)?,
+                "sample_method" => sample_method = utf8_input(nt)?,
+                "scheduler" => scheduler = utf8_input(nt)?,
+                "steps" => steps = scalar_i64(nt)?,
+                "seed" => seed = scalar_i64(nt)?,
+                "width" => width = scalar_i64(nt)?,
+                "height" => height = scalar_i64(nt)?,
+                "cfg" => cfg = scalar_f32(nt)?,
+                "image" => {
+                    return Err(err(
+                        "input \"image\" belongs to UPSCALER volumes - this volume is a \
+                         generation model (prompt in, image out)",
+                    ));
+                }
+                other => return Err(err(format!("unknown input \"{other}\""))),
+            }
+        }
+        let prompt = prompt.ok_or_else(|| err("missing \"prompt\" input"))?;
+        if !(1..=200).contains(&steps) {
+            return Err(err(format!("steps {steps} out of range 1..=200")));
+        }
+        if !(64..=4096).contains(&width) || !(64..=4096).contains(&height) {
+            return Err(err(format!("size {width}x{height} out of range 64..=4096")));
+        }
+
+        let prompt_c = CString::new(prompt).map_err(|_| err("prompt contains NUL"))?;
+        let negative_c = CString::new(negative).map_err(|_| err("negative_prompt contains NUL"))?;
+        let method_c = CString::new(sample_method).map_err(|_| err("sample_method contains NUL"))?;
+        let scheduler_c = CString::new(scheduler).map_err(|_| err("scheduler contains NUL"))?;
+
+        let mut rgb = vec![0u8; (width * height * 3) as usize];
+        let rc = {
+            let raw = self.model.raw.lock().unwrap();
+            unsafe {
+                esd_txt2img(
+                    *raw as *mut c_void,
+                    prompt_c.as_ptr(),
+                    negative_c.as_ptr(),
+                    width as i32,
+                    height as i32,
+                    steps as i32,
+                    cfg,
+                    seed,
+                    method_c.as_ptr(),
+                    scheduler_c.as_ptr(),
+                    rgb.as_mut_ptr(),
+                )
+            }
+        };
+        if rc != 0 {
+            return Err(err(format!("txt2img failed (rc {rc}): {}", last_error())));
+        }
+        Ok(Tensor {
+            dimensions: vec![1, height as u32, width as u32, 3],
+            ty: TensorType::U8,
+            data: rgb,
+        })
+    }
+}
+
+impl BackendExecutionContext for SdExecutionContext {
+    fn set_input(&mut self, _id: Id, _tensor: &Tensor) -> Result<(), BackendError> {
+        Err(err("the sd backend is WIT-only: pass named tensors to compute()"))
+    }
+
+    fn compute(
+        &mut self,
+        inputs: Option<Vec<NamedTensor>>,
+    ) -> Result<Option<Vec<NamedTensor>>, BackendError> {
+        let Some(inputs) = inputs else {
+            return Err(err("the sd backend is WIT-only: pass named tensors to compute()"));
+        };
+        let image = self.generate(&inputs)?;
+        self.image = Some(image.clone());
+        Ok(Some(vec![NamedTensor { name: "image".to_string(), tensor: image }]))
+    }
+
+    fn get_output(&mut self, id: Id) -> Result<Tensor, BackendError> {
+        match id {
+            Id::Index(0) => {}
+            Id::Name(ref n) if n == "image" => {}
+            other => return Err(err(format!("unknown output {other:?} (expected \"image\")"))),
+        }
+        self.image
+            .clone()
+            .ok_or_else(|| err("no image available (has `compute` been called?)"))
+    }
+}
+
+// ------------------------------------------------------- upscaler volumes --
+
+/// Output pixel budget for one upscale: 8192x8192 RGB (a 2048px input at 4x)
+/// is ~201 MB of tensor either way; anything past that stops being a
+/// per-request payload and starts being a memory event.
+const MAX_UPSCALE_OUT_BYTES: u64 = 3 * 8192 * 8192;
+
+fn load_upscaler(dir: &Path, model: &Path, use_gpu: bool) -> Result<Graph, BackendError> {
+    let Some(syms) = upscaler_syms() else {
+        return Err(err(format!(
+            "sd: {} is an upscaler volume (ENCLAVE_SD_UPSCALE_FILE) but this \
+             toolchain's sd shim predates the upscaler entry points - rebuild \
+             the llamacpp toolchain tarball to serve it",
+            dir.display()
+        )));
+    };
+    let n_threads: i32 = std::env::var("ENCLAVE_SD_N_THREADS")
+        .ok()
+        .and_then(|v| v.parse().ok())
+        .unwrap_or(0);
+    let tile: i32 = std::env::var("ENCLAVE_SD_UPSCALE_TILE")
+        .ok()
+        .and_then(|v| v.trim().parse().ok())
+        .unwrap_or(256);
+    let model_c = cstring(model)?;
+    let handle = unsafe { (syms.load)(model_c.as_ptr(), n_threads, use_gpu as i32, tile) };
+    if handle.is_null() {
+        return Err(err(format!(
+            "stable-diffusion.cpp failed to load upscaler {}: {}",
+            model.display(),
+            last_error()
+        )));
+    }
+    let scale = unsafe { (syms.scale)(handle) };
+    let graph: Box<dyn BackendGraph> = Box::new(SdUpscalerGraph(Arc::new(SdUpscalerModel {
+        raw: Mutex::new(handle as usize),
+        scale: scale.clamp(1, 16) as u32,
+    })));
+    Ok(graph.into())
+}
+
+/// A loaded ESRGAN upscaler. Like generation, not documented thread-safe -
+/// the raw handle lives behind a Mutex, one upscale at a time per model.
+struct SdUpscalerModel {
+    raw: Mutex<usize>,
+    /// the model's native factor (1/2/4), read from the weights at load
+    scale: u32,
+}
+impl Drop for SdUpscalerModel {
+    fn drop(&mut self) {
+        // syms were resolved at load or this model would not exist
+        if let Some(syms) = upscaler_syms() {
+            let raw = *self.raw.lock().unwrap();
+            unsafe { (syms.free)(raw as *mut c_void) };
+        }
+    }
+}
+
+struct SdUpscalerGraph(Arc<SdUpscalerModel>);
+
+impl BackendGraph for SdUpscalerGraph {
+    fn init_execution_context(&self) -> Result<ExecutionContext, BackendError> {
+        let ctx: Box<dyn BackendExecutionContext> = Box::new(SdUpscalerExecutionContext {
+            model: self.0.clone(),
+            image: None,
+        });
+        Ok(ctx.into())
+    }
+}
+
+struct SdUpscalerExecutionContext {
+    model: Arc<SdUpscalerModel>,
+    image: Option<Tensor>, // last compute's output for get_output
+}
+
+impl SdUpscalerExecutionContext {
+    fn upscale(&mut self, inputs: &[NamedTensor]) -> Result<Tensor, BackendError> {
+        let mut image = None;
+        for nt in inputs {
+            match nt.name.as_str() {
+                "image" => image = Some(nt),
+                other => {
+                    return Err(err(format!(
+                        "unknown input \"{other}\" - this volume is an ESRGAN upscaler: \
+                         pass \"image\" U8 [1, h, w, 3] (prompts belong on the \
+                         generation volumes)"
+                    )));
+                }
+            }
+        }
+        let nt = image.ok_or_else(|| {
+            err("missing \"image\" input - this volume is an ESRGAN upscaler: \
+                 pass \"image\" U8 [1, h, w, 3] and read back the upscaled \"image\"")
+        })?;
+        if nt.tensor.ty != TensorType::U8 {
+            return Err(BackendError::UnsupportedTensorType(format!(
+                "{:?} (input \"image\" wants U8 RGB row-major)",
+                nt.tensor.ty
+            )));
+        }
+        let (h, w) = match nt.tensor.dimensions[..] {
+            [1, h, w, 3] | [h, w, 3] => (h as u64, w as u64),
+            ref d => {
+                return Err(err(format!(
+                    "input \"image\" wants dimensions [1, h, w, 3], got {d:?}"
+                )));
+            }
+        };
+        if !(16..=4096).contains(&w) || !(16..=4096).contains(&h) {
+            return Err(err(format!("image size {w}x{h} out of range 16..=4096")));
+        }
+        if nt.tensor.data.len() as u64 != w * h * 3 {
+            return Err(err(format!(
+                "input \"image\" carries {} bytes, expected {} ({w}x{h}x3)",
+                nt.tensor.data.len(),
+                w * h * 3
+            )));
+        }
+        let scale = self.model.scale as u64;
+        let (out_w, out_h) = (w * scale, h * scale);
+        if out_w * out_h * 3 > MAX_UPSCALE_OUT_BYTES {
+            return Err(err(format!(
+                "{w}x{h} at this model's {scale}x would be {out_w}x{out_h} - past the \
+                 8192x8192 output budget; downscale the input or tile it client-side"
+            )));
+        }
+        let syms = upscaler_syms().expect("resolved at load");
+        let mut rgb = vec![0u8; (out_w * out_h * 3) as usize];
+        let rc = {
+            let raw = self.model.raw.lock().unwrap();
+            unsafe {
+                (syms.upscale)(
+                    *raw as *mut c_void,
+                    nt.tensor.data.as_ptr(),
+                    w as i32,
+                    h as i32,
+                    rgb.as_mut_ptr(),
+                )
+            }
+        };
+        if rc != 0 {
+            return Err(err(format!("upscale failed (rc {rc}): {}", last_error())));
+        }
+        Ok(Tensor {
+            dimensions: vec![1, out_h as u32, out_w as u32, 3],
+            ty: TensorType::U8,
+            data: rgb,
+        })
+    }
+}
+
+impl BackendExecutionContext for SdUpscalerExecutionContext {
+    fn set_input(&mut self, _id: Id, _tensor: &Tensor) -> Result<(), BackendError> {
+        Err(err("the sd backend is WIT-only: pass named tensors to compute()"))
+    }
+
+    fn compute(
+        &mut self,
+        inputs: Option<Vec<NamedTensor>>,
+    ) -> Result<Option<Vec<NamedTensor>>, BackendError> {
+        let Some(inputs) = inputs else {
+            return Err(err("the sd backend is WIT-only: pass named tensors to compute()"));
+        };
+        let image = self.upscale(&inputs)?;
+        self.image = Some(image.clone());
+        Ok(Some(vec![NamedTensor { name: "image".to_string(), tensor: image }]))
+    }
+
+    fn get_output(&mut self, id: Id) -> Result<Tensor, BackendError> {
+        match id {
+            Id::Index(0) => {}
+            Id::Name(ref n) if n == "image" => {}
+            other => return Err(err(format!("unknown output {other:?} (expected \"image\")"))),
+        }
+        self.image
+            .clone()
+            .ok_or_else(|| err("no image available (has `compute` been called?)"))
+    }
+}
diff --git a/crates/wasi-nn/src/lib.rs b/crates/wasi-nn/src/lib.rs
index 3dd6190c17..41410bbaf1 100644
--- a/crates/wasi-nn/src/lib.rs
+++ b/crates/wasi-nn/src/lib.rs
@@ -30,11 +30,23 @@ pub fn preload_registry(preload_graphs: &[(String, String)]) -> wasmtime::Result
     let mut backends = backend::list();
     let mut registry = InMemoryRegistry::new();
     for (kind, path) in preload_graphs {
-        let kind_ = kind.parse()?;
-        let backend = backends
-            .iter_mut()
-            .find(|b| b.encoding() == kind_)
-            .ok_or(format_err!("unsupported backend: {kind}"))?
+        // Enclave: preload-only backends (kinds with no guest-visible
+        // GraphEncoding case, e.g. "sd") match by their kind string first;
+        // everything else resolves through the encoding as before.
+        let idx = match backends
+            .iter()
+            .position(|b| !b.preload_kind().is_empty() && b.preload_kind().eq_ignore_ascii_case(kind))
+        {
+            Some(i) => i,
+            None => {
+                let kind_ = kind.parse()?;
+                backends
+                    .iter()
+                    .position(|b| b.encoding() == kind_)
+                    .ok_or(format_err!("unsupported backend: {kind}"))?
+            }
+        };
+        let backend = backends[idx]
             .as_dir_loadable()
             .ok_or(format_err!("{kind} does not support directory loading"))?;
         // Enclave: a graph that fails to load (typically CUDA OOM - the
